{
  "meta": {
    "document_version": "2.0.0",
    "document_type": "Full Technical Specification - Ready for AI Implementation",
    "project_name": "Spare Parts ERP - نظام إدارة قطع الغيار",
    "project_name_en": "Spare Parts ERP",
    "last_updated": "2024-01-01",
    "target_developer": "AI Implementation System",
    "instruction": "هذا الملف هو المصدر الوحيد للحقيقة - لا تفترض أي شيء خارج هذا الملف - كل شيء مذكور هنا يجب تنفيذه بالضبط"
  },

  "project_overview": {
    "description": "نظام إدارة موارد مؤسسية متكامل ERP مخصص للشركات متعددة الفروع",
    "type": "Desktop Application",
    "deployment": "Windows 10 / Windows 11 فقط",
    "architecture": "Offline-First Hybrid (Local SQLite + Cloud Supabase)",
    "primary_language": "Arabic RTL",
    "secondary_language": "French",
    "multi_branch": true,
    "multi_user": true,
    "internet_required": false,
    "works_offline": true,
    "auto_sync_when_online": true,
    "design_style": "Dark Professional Glassmorphism - NO gold colors - Corporate Blue/Gray palette"
  },

  "tech_stack": {
    "frontend": {
      "framework": "React",
      "version": "18.2.0",
      "language": "TypeScript",
      "typescript_version": "5.3.0",
      "build_tool": "Vite",
      "vite_version": "5.0.0",
      "styling": "Tailwind CSS",
      "tailwind_version": "3.4.0",
      "state_management": "Zustand",
      "zustand_version": "4.4.0",
      "routing": "React Router",
      "react_router_version": "6.21.0",
      "form_management": "React Hook Form",
      "react_hook_form_version": "7.49.0",
      "validation": "Zod",
      "zod_version": "3.22.0",
      "icons": "Lucide React",
      "lucide_version": "0.303.0",
      "charts": "Recharts",
      "recharts_version": "2.10.0",
      "printing": "React To Print",
      "react_to_print_version": "2.15.0",
      "barcode_display": "react-barcode",
      "barcode_display_version": "1.5.0",
      "barcode_scanner": "quagga2",
      "quagga2_version": "0.76.0",
      "date_handling": "date-fns",
      "date_fns_version": "3.0.0",
      "notifications": "react-hot-toast",
      "notifications_version": "2.4.0",
      "excel_export": "xlsx",
      "xlsx_version": "0.18.5",
      "pdf_export": "jspdf + jspdf-autotable",
      "jspdf_version": "2.5.1",
      "number_formatting": "numeral",
      "numeral_version": "2.0.6"
    },

    "desktop_wrapper": {
      "framework": "Electron",
      "version": "28.0.0",
      "builder": "Electron Builder",
      "electron_builder_version": "24.9.0",
      "auto_updater": "Electron Updater",
      "ipc_pattern": "Main Process <-> Renderer Process via IPC",
      "node_integration": false,
      "context_isolation": true,
      "preload_scripts": true
    },

    "local_database": {
      "engine": "SQLite3",
      "library": "better-sqlite3",
      "better_sqlite3_version": "9.4.0",
      "query_builder": "Knex.js",
      "knex_version": "3.1.0",
      "file_name": "spare_parts_erp.db",
      "file_location": "app.getPath('userData') + /SparePartsERP/spare_parts_erp.db",
      "full_path_example": "C:/Users/{username}/AppData/Roaming/SparePartsERP/spare_parts_erp.db",
      "backup_folder": "C:/Users/{username}/AppData/Roaming/SparePartsERP/backups/",
      "backup_format": "spare_parts_erp_backup_{YYYY-MM-DD_HH-mm}.db",
      "wal_mode": true,
      "foreign_keys": true,
      "transactions": "Atomic - all or nothing"
    },

    "cloud_database": {
      "provider": "Supabase",
      "database": "PostgreSQL 15",
      "realtime": true,
      "auth": "Supabase Auth",
      "storage": "Supabase Storage (for backups)",
      "sdk": "@supabase/supabase-js",
      "supabase_sdk_version": "2.39.0",
      "connection": "REST API + WebSocket for realtime"
    },

    "security_libraries": {
      "password_hashing": "bcryptjs",
      "bcryptjs_version": "2.4.3",
      "bcrypt_cost_factor": 12,
      "token": "jsonwebtoken",
      "jsonwebtoken_version": "9.0.2",
      "token_expiry": "8h",
      "encryption": "crypto-js",
      "cryptojs_version": "4.2.0",
      "encryption_algorithm": "AES-256",
      "secure_storage": "electron-store",
      "electron_store_version": "8.1.0"
    }
  },

  "project_structure": {
    "root": "spare-parts-erp/",
    "folders": {
      "electron/": {
        "main.ts": "Electron main process - window creation - IPC handlers",
        "preload.ts": "Secure bridge between main and renderer",
        "ipc/": {
          "database.ipc.ts": "كل handlers قاعدة البيانات",
          "print.ipc.ts": "handlers الطباعة",
          "backup.ipc.ts": "handlers النسخ الاحتياطي",
          "auth.ipc.ts": "handlers المصادقة"
        }
      },
      "src/": {
        "main.tsx": "نقطة دخول React",
        "App.tsx": "Router الرئيسي + Auth Guard",
        "components/": {
          "ui/": "مكونات UI عامة قابلة لإعادة الاستخدام",
          "layout/": {
            "Sidebar.tsx": "القائمة الجانبية",
            "TopBar.tsx": "الشريط العلوي",
            "MainLayout.tsx": "التخطيط الرئيسي"
          },
          "forms/": "نماذج مشتركة",
          "tables/": "جداول مشتركة",
          "modals/": "نوافذ منبثقة مشتركة",
          "print-templates/": "قوالب الطباعة"
        },
        "pages/": {
          "auth/": "صفحات تسجيل الدخول",
          "dashboard/": "الصفحة الرئيسية",
          "sales/": "صفحات المبيعات",
          "purchases/": "صفحات المشتريات",
          "inventory/": "صفحات المخزون",
          "branches/": "صفحات الفروع",
          "customers/": "صفحات الزبائن",
          "suppliers/": "صفحات الموردين",
          "finance/": "صفحات المالية والأرباح",
          "reports/": "صفحات التقارير",
          "settings/": "صفحات الإعدادات"
        },
        "store/": {
          "auth.store.ts": "حالة المصادقة والمستخدم الحالي",
          "app.store.ts": "حالة التطبيق العامة",
          "sync.store.ts": "حالة المزامنة"
        },
        "services/": {
          "database/": {
            "index.ts": "اتصال قاعدة البيانات",
            "users.service.ts": "عمليات المستخدمين",
            "products.service.ts": "عمليات المنتجات",
            "sales.service.ts": "عمليات المبيعات",
            "purchases.service.ts": "عمليات المشتريات",
            "inventory.service.ts": "عمليات المخزون",
            "branches.service.ts": "عمليات الفروع",
            "finance.service.ts": "عمليات المالية",
            "reports.service.ts": "عمليات التقارير"
          },
          "sync.service.ts": "منطق المزامنة السحابية",
          "backup.service.ts": "منطق النسخ الاحتياطي",
          "print.service.ts": "منطق الطباعة"
        },
        "hooks/": {
          "useAuth.ts": "hook المصادقة",
          "usePermissions.ts": "hook الصلاحيات",
          "useSync.ts": "hook المزامنة",
          "useOffline.ts": "hook حالة الشبكة",
          "usePrint.ts": "hook الطباعة",
          "useBarcode.ts": "hook الباركود"
        },
        "utils/": {
          "calculations.ts": "كل الحسابات المالية",
          "formatters.ts": "تنسيق الأرقام والتواريخ والعملة",
          "validators.ts": "قواعد التحقق المشتركة",
          "invoice-number.ts": "توليد أرقام الفواتير",
          "permissions.ts": "منطق الصلاحيات",
          "encryption.ts": "تشفير البيانات الحساسة"
        },
        "types/": {
          "database.types.ts": "أنواع TypeScript لكل جدول",
          "api.types.ts": "أنواع API",
          "ui.types.ts": "أنواع UI"
        },
        "constants/": {
          "routes.ts": "ثوابت مسارات التنقل",
          "permissions.ts": "ثوابت الصلاحيات",
          "config.ts": "إعدادات التطبيق"
        },
        "i18n/": {
          "ar.json": "النصوص العربية",
          "fr.json": "النصوص الفرنسية",
          "i18n.config.ts": "إعداد نظام الترجمة"
        }
      },
      "database/": {
        "migrations/": "ملفات ترقية قاعدة البيانات",
        "seeds/": "بيانات أولية للاختبار",
        "schema.sql": "هيكل قاعدة البيانات الكامل"
      },
      "build/": "ملفات البناء النهائية",
      "dist/": "ملفات التوزيع"
    }
  },

  "design_system": {
    "theme": "Dark Professional",
    "style": "Glassmorphism - Corporate",
    "forbidden_colors": ["gold", "yellow as primary", "bright orange"],
    "color_palette": {
      "background_primary": "#0F172A",
      "background_secondary": "#1E293B",
      "background_card": "rgba(255, 255, 255, 0.05)",
      "background_card_hover": "rgba(255, 255, 255, 0.08)",
      "glass_border": "rgba(255, 255, 255, 0.10)",
      "glass_blur": "backdrop-filter: blur(12px)",
      "primary_blue": "#2563EB",
      "primary_blue_hover": "#1D4ED8",
      "success_green": "#22C55E",
      "danger_red": "#EF4444",
      "warning_amber": "#F59E0B",
      "info_cyan": "#06B6D4",
      "text_primary": "#F1F5F9",
      "text_secondary": "#94A3B8",
      "text_muted": "#475569",
      "border_default": "#334155",
      "sidebar_background": "#0D1526",
      "topbar_background": "rgba(15, 23, 42, 0.95)"
    },
    "typography": {
      "font_arabic": "Cairo, Noto Kufi Arabic, sans-serif",
      "font_french": "Inter, sans-serif",
      "font_numbers": "JetBrains Mono, monospace",
      "base_size": "14px",
      "scale": [10, 12, 14, 16, 18, 20, 24, 28, 32, 36, 48]
    },
    "border_radius": {
      "small": "6px",
      "medium": "10px",
      "large": "16px",
      "full": "9999px"
    },
    "spacing": "4px base unit (4, 8, 12, 16, 20, 24, 32, 40, 48, 64)",
    "shadows": {
      "card": "0 4px 24px rgba(0,0,0,0.3)",
      "modal": "0 20px 60px rgba(0,0,0,0.5)",
      "button": "0 2px 8px rgba(37,99,235,0.3)"
    },
    "direction": "RTL for Arabic - LTR for French",
    "transitions": "all 200ms ease-in-out",
    "glassmorphism_recipe": {
      "background": "rgba(255,255,255,0.05)",
      "border": "1px solid rgba(255,255,255,0.10)",
      "backdrop_filter": "blur(12px)",
      "box_shadow": "0 4px 24px rgba(0,0,0,0.3)"
    }
  },

  "database_schema": {
    "naming_convention": "snake_case for all tables and columns",
    "encoding": "UTF-8",
    "timezone": "Africa/Algiers or as configured",

    "tables": {

      "users": {
        "purpose": "جميع مستخدمي النظام",
        "columns": {
          "id": { "type": "INTEGER", "constraints": "PRIMARY KEY AUTOINCREMENT" },
          "username": { "type": "TEXT", "constraints": "NOT NULL UNIQUE", "min_length": 3, "max_length": 50 },
          "password_hash": { "type": "TEXT", "constraints": "NOT NULL", "note": "bcrypt hash - never plain text" },
          "full_name": { "type": "TEXT", "constraints": "NOT NULL", "max_length": 100 },
          "full_name_fr": { "type": "TEXT", "constraints": "NULL", "max_length": 100 },
          "role": { "type": "TEXT", "constraints": "NOT NULL CHECK(role IN ('owner','manager','accountant','employee'))" },
          "branch_id": { "type": "INTEGER", "constraints": "REFERENCES branches(id)", "note": "NULL يعني صلاحية على كل الفروع" },
          "is_active": { "type": "INTEGER", "constraints": "NOT NULL DEFAULT 1 CHECK(is_active IN (0,1))" },
          "pin_code": { "type": "TEXT", "constraints": "NULL", "note": "PIN مشفر للوصول للبيانات الحساسة" },
          "created_at": { "type": "TEXT", "constraints": "NOT NULL DEFAULT (datetime('now'))" },
          "updated_at": { "type": "TEXT", "constraints": "NOT NULL DEFAULT (datetime('now'))" },
          "last_login": { "type": "TEXT", "constraints": "NULL" },
          "cloud_id": { "type": "TEXT", "constraints": "NULL UNIQUE", "note": "Supabase user ID" }
        },
        "indexes": ["CREATE INDEX idx_users_username ON users(username)", "CREATE INDEX idx_users_branch ON users(branch_id)"]
      },

      "branches": {
        "purpose": "الفروع والمخازن",
        "columns": {
          "id": { "type": "INTEGER", "constraints": "PRIMARY KEY AUTOINCREMENT" },
          "code": { "type": "TEXT", "constraints": "NOT NULL UNIQUE", "example": "HQ, BR001, BR002" },
          "name": { "type": "TEXT", "constraints": "NOT NULL", "max_length": 100 },
          "name_fr": { "type": "TEXT", "constraints": "NULL" },
          "address": { "type": "TEXT", "constraints": "NULL" },
          "phone": { "type": "TEXT", "constraints": "NULL" },
          "manager_id": { "type": "INTEGER", "constraints": "REFERENCES users(id) NULL" },
          "is_headquarters": { "type": "INTEGER", "constraints": "NOT NULL DEFAULT 0 CHECK(IN (0,1))" },
          "is_active": { "type": "INTEGER", "constraints": "NOT NULL DEFAULT 1" },
          "created_at": { "type": "TEXT", "constraints": "NOT NULL DEFAULT (datetime('now'))" },
          "synced": { "type": "INTEGER", "constraints": "NOT NULL DEFAULT 0" },
          "cloud_id": { "type": "TEXT", "constraints": "NULL UNIQUE" }
        }
      },

      "categories": {
        "purpose": "تصنيفات المنتجات - تدعم التصنيف الهرمي",
        "columns": {
          "id": { "type": "INTEGER", "constraints": "PRIMARY KEY AUTOINCREMENT" },
          "name": { "type": "TEXT", "constraints": "NOT NULL", "max_length": 100 },
          "name_fr": { "type": "TEXT", "constraints": "NULL" },
          "parent_id": { "type": "INTEGER", "constraints": "REFERENCES categories(id) NULL", "note": "NULL = تصنيف رئيسي" },
          "sort_order": { "type": "INTEGER", "constraints": "NOT NULL DEFAULT 0" },
          "is_active": { "type": "INTEGER", "constraints": "NOT NULL DEFAULT 1" },
          "created_at": { "type": "TEXT", "constraints": "NOT NULL DEFAULT (datetime('now'))" },
          "synced": { "type": "INTEGER", "constraints": "NOT NULL DEFAULT 0" }
        }
      },

      "products": {
        "purpose": "كتالوج المنتجات الكامل",
        "columns": {
          "id": { "type": "INTEGER", "constraints": "PRIMARY KEY AUTOINCREMENT" },
          "barcode": { "type": "TEXT", "constraints": "UNIQUE NULL" },
          "internal_code": { "type": "TEXT", "constraints": "UNIQUE NULL", "note": "كود داخلي اختياري" },
          "name": { "type": "TEXT", "constraints": "NOT NULL", "max_length": 200 },
          "name_fr": { "type": "TEXT", "constraints": "NULL", "max_length": 200 },
          "category_id": { "type": "INTEGER", "constraints": "REFERENCES categories(id) NULL" },
          "unit": { "type": "TEXT", "constraints": "NOT NULL DEFAULT 'قطعة'" },
          "purchase_price": { "type": "REAL", "constraints": "NOT NULL DEFAULT 0 CHECK(purchase_price >= 0)" },
          "wholesale_price": { "type": "REAL", "constraints": "NOT NULL DEFAULT 0 CHECK(wholesale_price >= 0)" },
          "half_wholesale_price": { "type": "REAL", "constraints": "NOT NULL DEFAULT 0" },
          "retail_price": { "type": "REAL", "constraints": "NOT NULL DEFAULT 0 CHECK(retail_price >= 0)" },
          "min_stock_level": { "type": "REAL", "constraints": "NOT NULL DEFAULT 0" },
          "description": { "type": "TEXT", "constraints": "NULL" },
          "is_active": { "type": "INTEGER", "constraints": "NOT NULL DEFAULT 1" },
          "created_at": { "type": "TEXT", "constraints": "NOT NULL DEFAULT (datetime('now'))" },
          "updated_at": { "type": "TEXT", "constraints": "NOT NULL DEFAULT (datetime('now'))" },
          "synced": { "type": "INTEGER", "constraints": "NOT NULL DEFAULT 0" },
          "cloud_id": { "type": "TEXT", "constraints": "NULL UNIQUE" }
        },
        "indexes": [
          "CREATE INDEX idx_products_barcode ON products(barcode)",
          "CREATE INDEX idx_products_name ON products(name)",
          "CREATE INDEX idx_products_category ON products(category_id)"
        ],
        "business_rules": {
          "rule_1": "retail_price يجب أن يكون >= wholesale_price",
          "rule_2": "wholesale_price يجب أن يكون >= purchase_price",
          "rule_3": "half_wholesale_price يجب أن يكون بين wholesale_price وretail_price",
          "rule_4": "لا يمكن حذف منتج موجود في فاتورة مؤكدة - يُعطّل فقط"
        }
      },

      "stock": {
        "purpose": "كمية المخزون لكل منتج في كل فرع",
        "columns": {
          "id": { "type": "INTEGER", "constraints": "PRIMARY KEY AUTOINCREMENT" },
          "product_id": { "type": "INTEGER", "constraints": "NOT NULL REFERENCES products(id)" },
          "branch_id": { "type": "INTEGER", "constraints": "NOT NULL REFERENCES branches(id)" },
          "quantity": { "type": "REAL", "constraints": "NOT NULL DEFAULT 0" },
          "updated_at": { "type": "TEXT", "constraints": "NOT NULL DEFAULT (datetime('now'))" }
        },
        "constraints": "UNIQUE(product_id, branch_id)",
        "indexes": ["CREATE INDEX idx_stock_product ON stock(product_id)", "CREATE INDEX idx_stock_branch ON stock(branch_id)"],
        "business_rules": {
          "rule_1": "لا تسمح بـ quantity سالبة - أظهر تحذيراً للمستخدم",
          "rule_2": "عند إضافة منتج جديد أنشئ سجل stock بكمية 0 لكل فرع"
        }
      },

      "customers": {
        "purpose": "زبائن الشركة",
        "columns": {
          "id": { "type": "INTEGER", "constraints": "PRIMARY KEY AUTOINCREMENT" },
          "code": { "type": "TEXT", "constraints": "UNIQUE NOT NULL", "auto_generated": "CLI-0001" },
          "name": { "type": "TEXT", "constraints": "NOT NULL", "max_length": 150 },
          "name_fr": { "type": "TEXT", "constraints": "NULL" },
          "phone": { "type": "TEXT", "constraints": "NULL" },
          "phone2": { "type": "TEXT", "constraints": "NULL" },
          "address": { "type": "TEXT", "constraints": "NULL" },
          "email": { "type": "TEXT", "constraints": "NULL" },
          "balance": { "type": "REAL", "constraints": "NOT NULL DEFAULT 0", "note": "양수 = يدين لنا - سالب = نحن ندين له" },
          "credit_limit": { "type": "REAL", "constraints": "NOT NULL DEFAULT 0", "note": "0 = بدون حد" },
          "branch_id": { "type": "INTEGER", "constraints": "REFERENCES branches(id) NULL" },
          "is_active": { "type": "INTEGER", "constraints": "NOT NULL DEFAULT 1" },
          "notes": { "type": "TEXT", "constraints": "NULL" },
          "created_at": { "type": "TEXT", "constraints": "NOT NULL DEFAULT (datetime('now'))" },
          "updated_at": { "type": "TEXT", "constraints": "NOT NULL DEFAULT (datetime('now'))" },
          "synced": { "type": "INTEGER", "constraints": "NOT NULL DEFAULT 0" },
          "cloud_id": { "type": "TEXT", "constraints": "NULL UNIQUE" }
        },
        "indexes": ["CREATE INDEX idx_customers_name ON customers(name)", "CREATE INDEX idx_customers_code ON customers(code)"]
      },

      "suppliers": {
        "purpose": "موردو الشركة",
        "columns": {
          "id": { "type": "INTEGER", "constraints": "PRIMARY KEY AUTOINCREMENT" },
          "code": { "type": "TEXT", "constraints": "UNIQUE NOT NULL", "auto_generated": "SUP-0001" },
          "name": { "type": "TEXT", "constraints": "NOT NULL", "max_length": 150 },
          "name_fr": { "type": "TEXT", "constraints": "NULL" },
          "phone": { "type": "TEXT", "constraints": "NULL" },
          "phone2": { "type": "TEXT", "constraints": "NULL" },
          "address": { "type": "TEXT", "constraints": "NULL" },
          "email": { "type": "TEXT", "constraints": "NULL" },
          "balance": { "type": "REAL", "constraints": "NOT NULL DEFAULT 0", "note": "양수 = نحن ندين لهم" },
          "is_active": { "type": "INTEGER", "constraints": "NOT NULL DEFAULT 1" },
          "notes": { "type": "TEXT", "constraints": "NULL" },
          "created_at": { "type": "TEXT", "constraints": "NOT NULL DEFAULT (datetime('now'))" },
          "updated_at": { "type": "TEXT", "constraints": "NOT NULL DEFAULT (datetime('now'))" },
          "synced": { "type": "INTEGER", "constraints": "NOT NULL DEFAULT 0" },
          "cloud_id": { "type": "TEXT", "constraints": "NULL UNIQUE" }
        }
      },

      "sales_invoices": {
        "purpose": "رؤوس فواتير البيع",
        "columns": {
          "id": { "type": "INTEGER", "constraints": "PRIMARY KEY AUTOINCREMENT" },
          "invoice_number": { "type": "TEXT", "constraints": "UNIQUE NOT NULL", "format": "VNT-{BRANCH_CODE}-{YYYY}-{NNNN}", "example": "VNT-HQ-2024-0001" },
          "sale_type": { "type": "TEXT", "constraints": "NOT NULL CHECK(IN ('retail','wholesale','half_wholesale'))" },
          "customer_id": { "type": "INTEGER", "constraints": "REFERENCES customers(id) NULL", "note": "NULL = بيع نقدي بدون زبون محدد" },
          "branch_id": { "type": "INTEGER", "constraints": "NOT NULL REFERENCES branches(id)" },
          "user_id": { "type": "INTEGER", "constraints": "NOT NULL REFERENCES users(id)" },
          "date": { "type": "TEXT", "constraints": "NOT NULL", "format": "YYYY-MM-DD" },
          "time": { "type": "TEXT", "constraints": "NOT NULL DEFAULT (time('now'))" },
          "subtotal": { "type": "REAL", "constraints": "NOT NULL DEFAULT 0", "note": "مجموع بنود الفاتورة قبل الخصم العام" },
          "global_discount_type": { "type": "TEXT", "constraints": "CHECK(IN ('percent','amount')) DEFAULT 'percent'" },
          "global_discount_value": { "type": "REAL", "constraints": "NOT NULL DEFAULT 0" },
          "global_discount_amount": { "type": "REAL", "constraints": "NOT NULL DEFAULT 0", "note": "القيمة المحسوبة للخصم" },
          "total_before_tax": { "type": "REAL", "constraints": "NOT NULL DEFAULT 0" },
          "tax_percent": { "type": "REAL", "constraints": "NOT NULL DEFAULT 0" },
          "tax_amount": { "type": "REAL", "constraints": "NOT NULL DEFAULT 0" },
          "total": { "type": "REAL", "constraints": "NOT NULL DEFAULT 0", "note": "المبلغ النهائي المستحق" },
          "paid": { "type": "REAL", "constraints": "NOT NULL DEFAULT 0" },
          "remaining": { "type": "REAL", "constraints": "NOT NULL DEFAULT 0" },
          "payment_method": { "type": "TEXT", "constraints": "CHECK(IN ('cash','credit','mixed')) DEFAULT 'cash'" },
          "status": { "type": "TEXT", "constraints": "NOT NULL CHECK(IN ('draft','confirmed','cancelled')) DEFAULT 'draft'" },
          "cancel_reason": { "type": "TEXT", "constraints": "NULL" },
          "notes": { "type": "TEXT", "constraints": "NULL" },
          "printed_count": { "type": "INTEGER", "constraints": "NOT NULL DEFAULT 0" },
          "created_at": { "type": "TEXT", "constraints": "NOT NULL DEFAULT (datetime('now'))" },
          "updated_at": { "type": "TEXT", "constraints": "NOT NULL DEFAULT (datetime('now'))" },
          "synced": { "type": "INTEGER", "constraints": "NOT NULL DEFAULT 0" },
          "cloud_id": { "type": "TEXT", "constraints": "NULL UNIQUE" }
        },
        "indexes": [
          "CREATE INDEX idx_sales_number ON sales_invoices(invoice_number)",
          "CREATE INDEX idx_sales_date ON sales_invoices(date)",
          "CREATE INDEX idx_sales_customer ON sales_invoices(customer_id)",
          "CREATE INDEX idx_sales_branch ON sales_invoices(branch_id)",
          "CREATE INDEX idx_sales_status ON sales_invoices(status)"
        ]
      },

      "sales_invoice_items": {
        "purpose": "بنود فواتير البيع",
        "columns": {
          "id": { "type": "INTEGER", "constraints": "PRIMARY KEY AUTOINCREMENT" },
          "invoice_id": { "type": "INTEGER", "constraints": "NOT NULL REFERENCES sales_invoices(id) ON DELETE CASCADE" },
          "product_id": { "type": "INTEGER", "constraints": "NOT NULL REFERENCES products(id)" },
          "product_name_snapshot": { "type": "TEXT", "constraints": "NOT NULL", "note": "نسخة من اسم المنتج وقت الفاتورة" },
          "product_barcode_snapshot": { "type": "TEXT", "constraints": "NULL" },
          "quantity": { "type": "REAL", "constraints": "NOT NULL CHECK(quantity > 0)" },
          "unit": { "type": "TEXT", "constraints": "NOT NULL" },
          "unit_price": { "type": "REAL", "constraints": "NOT NULL CHECK(unit_price >= 0)" },
          "purchase_price_snapshot": { "type": "REAL", "constraints": "NOT NULL", "note": "سعر الشراء وقت البيع - ضروري لحساب الربح لاحقاً" },
          "item_discount_type": { "type": "TEXT", "constraints": "CHECK(IN ('percent','amount')) DEFAULT 'percent'" },
          "item_discount_value": { "type": "REAL", "constraints": "NOT NULL DEFAULT 0" },
          "item_discount_amount": { "type": "REAL", "constraints": "NOT NULL DEFAULT 0" },
          "total": { "type": "REAL", "constraints": "NOT NULL", "note": "quantity * unit_price - item_discount_amount" },
          "sort_order": { "type": "INTEGER", "constraints": "NOT NULL DEFAULT 0" }
        },
        "indexes": ["CREATE INDEX idx_sales_items_invoice ON sales_invoice_items(invoice_id)"]
      },

      "purchase_invoices": {
        "purpose": "رؤوس فواتير الشراء",
        "columns": {
          "id": { "type": "INTEGER", "constraints": "PRIMARY KEY AUTOINCREMENT" },
          "invoice_number": { "type": "TEXT", "constraints": "UNIQUE NOT NULL", "format": "ACH-{BRANCH_CODE}-{YYYY}-{NNNN}" },
          "supplier_invoice_number": { "type": "TEXT", "constraints": "NULL", "note": "رقم الفاتورة الأصلية من المورد" },
          "supplier_id": { "type": "INTEGER", "constraints": "REFERENCES suppliers(id) NULL" },
          "branch_id": { "type": "INTEGER", "constraints": "NOT NULL REFERENCES branches(id)" },
          "user_id": { "type": "INTEGER", "constraints": "NOT NULL REFERENCES users(id)" },
          "date": { "type": "TEXT", "constraints": "NOT NULL" },
          "subtotal": { "type": "REAL", "constraints": "NOT NULL DEFAULT 0" },
          "discount_amount": { "type": "REAL", "constraints": "NOT NULL DEFAULT 0" },
          "tax_amount": { "type": "REAL", "constraints": "NOT NULL DEFAULT 0" },
          "total": { "type": "REAL", "constraints": "NOT NULL DEFAULT 0" },
          "paid": { "type": "REAL", "constraints": "NOT NULL DEFAULT 0" },
          "remaining": { "type": "REAL", "constraints": "NOT NULL DEFAULT 0" },
          "payment_method": { "type": "TEXT", "constraints": "CHECK(IN ('cash','credit','mixed')) DEFAULT 'cash'" },
          "status": { "type": "TEXT", "constraints": "NOT NULL CHECK(IN ('draft','confirmed','cancelled')) DEFAULT 'draft'" },
          "notes": { "type": "TEXT", "constraints": "NULL" },
          "created_at": { "type": "TEXT", "constraints": "NOT NULL DEFAULT (datetime('now'))" },
          "updated_at": { "type": "TEXT", "constraints": "NOT NULL DEFAULT (datetime('now'))" },
          "synced": { "type": "INTEGER", "constraints": "NOT NULL DEFAULT 0" },
          "cloud_id": { "type": "TEXT", "constraints": "NULL UNIQUE" }
        }
      },

      "purchase_invoice_items": {
        "purpose": "بنود فواتير الشراء",
        "columns": {
          "id": { "type": "INTEGER", "constraints": "PRIMARY KEY AUTOINCREMENT" },
          "invoice_id": { "type": "INTEGER", "constraints": "NOT NULL REFERENCES purchase_invoices(id) ON DELETE CASCADE" },
          "product_id": { "type": "INTEGER", "constraints": "NOT NULL REFERENCES products(id)" },
          "product_name_snapshot": { "type": "TEXT", "constraints": "NOT NULL" },
          "quantity": { "type": "REAL", "constraints": "NOT NULL CHECK(quantity > 0)" },
          "unit": { "type": "TEXT", "constraints": "NOT NULL" },
          "unit_price": { "type": "REAL", "constraints": "NOT NULL CHECK(unit_price >= 0)" },
          "total": { "type": "REAL", "constraints": "NOT NULL" }
        }
      },

      "branch_supply_orders": {
        "purpose": "أوامر تموين الفروع من المخزن الرئيسي",
        "columns": {
          "id": { "type": "INTEGER", "constraints": "PRIMARY KEY AUTOINCREMENT" },
          "order_number": { "type": "TEXT", "constraints": "UNIQUE NOT NULL", "format": "TMN-{YYYY}-{NNNN}" },
          "source_branch_id": { "type": "INTEGER", "constraints": "NOT NULL REFERENCES branches(id)", "note": "المخزن المرسل - عادة HQ" },
          "target_branch_id": { "type": "INTEGER", "constraints": "NOT NULL REFERENCES branches(id)", "note": "الفرع المستقبل" },
          "user_id": { "type": "INTEGER", "constraints": "NOT NULL REFERENCES users(id)" },
          "date": { "type": "TEXT", "constraints": "NOT NULL" },
          "status": { "type": "TEXT", "constraints": "NOT NULL CHECK(IN ('draft','shipped','received','confirmed','cancelled')) DEFAULT 'draft'" },
          "shipped_at": { "type": "TEXT", "constraints": "NULL" },
          "received_at": { "type": "TEXT", "constraints": "NULL" },
          "confirmed_at": { "type": "TEXT", "constraints": "NULL" },
          "confirmed_by_user_id": { "type": "INTEGER", "constraints": "REFERENCES users(id) NULL" },
          "total_items_count": { "type": "INTEGER", "constraints": "NOT NULL DEFAULT 0" },
          "has_discrepancy": { "type": "INTEGER", "constraints": "NOT NULL DEFAULT 0", "note": "1 إذا كان هناك فرق بين المرسل والمستلم" },
          "notes": { "type": "TEXT", "constraints": "NULL" },
          "created_at": { "type": "TEXT", "constraints": "NOT NULL DEFAULT (datetime('now'))" },
          "synced": { "type": "INTEGER", "constraints": "NOT NULL DEFAULT 0" },
          "cloud_id": { "type": "TEXT", "constraints": "NULL UNIQUE" }
        }
      },

      "branch_supply_items": {
        "purpose": "تفاصيل تموين الفروع",
        "columns": {
          "id": { "type": "INTEGER", "constraints": "PRIMARY KEY AUTOINCREMENT" },
          "order_id": { "type": "INTEGER", "constraints": "NOT NULL REFERENCES branch_supply_orders(id) ON DELETE CASCADE" },
          "product_id": { "type": "INTEGER", "constraints": "NOT NULL REFERENCES products(id)" },
          "product_name_snapshot": { "type": "TEXT", "constraints": "NOT NULL" },
          "quantity_requested": { "type": "REAL", "constraints": "NOT NULL CHECK(quantity_requested > 0)" },
          "quantity_shipped": { "type": "REAL", "constraints": "NOT NULL DEFAULT 0" },
          "quantity_received": { "type": "REAL", "constraints": "NULL", "note": "NULL حتى يؤكد الفرع الاستلام" },
          "discrepancy": { "type": "REAL", "constraints": "NOT NULL DEFAULT 0", "formula": "quantity_shipped - quantity_received" },
          "hq_stock_before": { "type": "REAL", "constraints": "NULL", "note": "مخزون المصدر قبل الشحن" },
          "branch_stock_before": { "type": "REAL", "constraints": "NULL", "note": "مخزون الفرع قبل الاستلام" }
        }
      },

      "payments": {
        "purpose": "الدفعات النقدية من الزبائن أو للموردين",
        "columns": {
          "id": { "type": "INTEGER", "constraints": "PRIMARY KEY AUTOINCREMENT" },
          "payment_number": { "type": "TEXT", "constraints": "UNIQUE NOT NULL", "format": "PAY-{YYYY}-{NNNN}" },
          "type": { "type": "TEXT", "constraints": "NOT NULL CHECK(IN ('customer_receipt','supplier_payment'))" },
          "direction": { "type": "TEXT", "constraints": "NOT NULL CHECK(IN ('in','out'))", "note": "in = استلمنا - out = دفعنا" },
          "party_id": { "type": "INTEGER", "constraints": "NOT NULL", "note": "customer_id أو supplier_id" },
          "party_type": { "type": "TEXT", "constraints": "NOT NULL CHECK(IN ('customer','supplier'))" },
          "invoice_id": { "type": "INTEGER", "constraints": "NULL", "note": "NULL إذا كانت دفعة عامة غير مرتبطة بفاتورة" },
          "invoice_type": { "type": "TEXT", "constraints": "CHECK(IN ('sale','purchase')) NULL" },
          "amount": { "type": "REAL", "constraints": "NOT NULL CHECK(amount > 0)" },
          "payment_method": { "type": "TEXT", "constraints": "NOT NULL CHECK(IN ('cash','bank_transfer','check'))" },
          "check_number": { "type": "TEXT", "constraints": "NULL" },
          "bank_reference": { "type": "TEXT", "constraints": "NULL" },
          "date": { "type": "TEXT", "constraints": "NOT NULL" },
          "branch_id": { "type": "INTEGER", "constraints": "NOT NULL REFERENCES branches(id)" },
          "user_id": { "type": "INTEGER", "constraints": "NOT NULL REFERENCES users(id)" },
          "notes": { "type": "TEXT", "constraints": "NULL" },
          "created_at": { "type": "TEXT", "constraints": "NOT NULL DEFAULT (datetime('now'))" },
          "synced": { "type": "INTEGER", "constraints": "NOT NULL DEFAULT 0" },
          "cloud_id": { "type": "TEXT", "constraints": "NULL UNIQUE" }
        }
      },

      "expenses": {
        "purpose": "المصروفات التشغيلية",
        "columns": {
          "id": { "type": "INTEGER", "constraints": "PRIMARY KEY AUTOINCREMENT" },
          "expense_number": { "type": "TEXT", "constraints": "UNIQUE NOT NULL", "format": "EXP-{YYYY}-{NNNN}" },
          "category": { "type": "TEXT", "constraints": "NOT NULL", "examples": "إيجار, رواتب, كهرباء, نقل, صيانة, أخرى" },
          "description": { "type": "TEXT", "constraints": "NOT NULL" },
          "amount": { "type": "REAL", "constraints": "NOT NULL CHECK(amount > 0)" },
          "date": { "type": "TEXT", "constraints": "NOT NULL" },
          "branch_id": { "type": "INTEGER", "constraints": "NOT NULL REFERENCES branches(id)" },
          "user_id": { "type": "INTEGER", "constraints": "NOT NULL REFERENCES users(id)" },
          "approved_by": { "type": "INTEGER", "constraints": "REFERENCES users(id) NULL" },
          "receipt_reference": { "type": "TEXT", "constraints": "NULL" },
          "notes": { "type": "TEXT", "constraints": "NULL" },
          "created_at": { "type": "TEXT", "constraints": "NOT NULL DEFAULT (datetime('now'))" },
          "synced": { "type": "INTEGER", "constraints": "NOT NULL DEFAULT 0" },
          "cloud_id": { "type": "TEXT", "constraints": "NULL UNIQUE" }
        }
      },

      "audit_log": {
        "purpose": "سجل تدقيق كامل لكل عملية في النظام",
        "note": "لا يُحذف منه أبداً - للقراءة فقط",
        "columns": {
          "id": { "type": "INTEGER", "constraints": "PRIMARY KEY AUTOINCREMENT" },
          "user_id": { "type": "INTEGER", "constraints": "REFERENCES users(id) NULL" },
          "username_snapshot": { "type": "TEXT", "constraints": "NOT NULL" },
          "action": { "type": "TEXT", "constraints": "NOT NULL", "values": "LOGIN, LOGOUT, CREATE, UPDATE, DELETE, PRINT, CANCEL, SYNC, BACKUP, RESTORE" },
          "table_name": { "type": "TEXT", "constraints": "NOT NULL" },
          "record_id": { "type": "INTEGER", "constraints": "NULL" },
          "description": { "type": "TEXT", "constraints": "NOT NULL", "note": "وصف العملية بالعربية" },
          "old_data": { "type": "TEXT", "constraints": "NULL", "format": "JSON string" },
          "new_data": { "type": "TEXT", "constraints": "NULL", "format": "JSON string" },
          "branch_id": { "type": "INTEGER", "constraints": "NULL" },
          "ip_address": { "type": "TEXT", "constraints": "NULL" },
          "app_version": { "type": "TEXT", "constraints": "NOT NULL" },
          "created_at": { "type": "TEXT", "constraints": "NOT NULL DEFAULT (datetime('now'))" }
        },
        "indexes": ["CREATE INDEX idx_audit_user ON audit_log(user_id)", "CREATE INDEX idx_audit_date ON audit_log(created_at)", "CREATE INDEX idx_audit_table ON audit_log(table_name)"]
      },

      "sync_queue": {
        "purpose": "قائمة انتظار المزامنة السحابية",
        "columns": {
          "id": { "type": "INTEGER", "constraints": "PRIMARY KEY AUTOINCREMENT" },
          "table_name": { "type": "TEXT", "constraints": "NOT NULL" },
          "record_id": { "type": "INTEGER", "constraints": "NOT NULL" },
          "operation": { "type": "TEXT", "constraints": "NOT NULL CHECK(IN ('INSERT','UPDATE','DELETE'))" },
          "data": { "type": "TEXT", "constraints": "NOT NULL", "format": "JSON string of the record" },
          "priority": { "type": "INTEGER", "constraints": "NOT NULL DEFAULT 5", "note": "1=عاجل 10=منخفض" },
          "attempts": { "type": "INTEGER", "constraints": "NOT NULL DEFAULT 0" },
          "max_attempts": { "type": "INTEGER", "constraints": "NOT NULL DEFAULT 5" },
          "last_attempt_at": { "type": "TEXT", "constraints": "NULL" },
          "last_error": { "type": "TEXT", "constraints": "NULL" },
          "status": { "type": "TEXT", "constraints": "NOT NULL CHECK(IN ('pending','processing','done','failed')) DEFAULT 'pending'" },
          "created_at": { "type": "TEXT", "constraints": "NOT NULL DEFAULT (datetime('now'))" }
        },
        "indexes": ["CREATE INDEX idx_sync_status ON sync_queue(status)", "CREATE INDEX idx_sync_table ON sync_queue(table_name)"]
      },

      "app_settings": {
        "purpose": "إعدادات التطبيق",
        "columns": {
          "id": { "type": "INTEGER", "constraints": "PRIMARY KEY AUTOINCREMENT" },
          "key": { "type": "TEXT", "constraints": "UNIQUE NOT NULL" },
          "value": { "type": "TEXT", "constraints": "NULL" },
          "type": { "type": "TEXT", "constraints": "NOT NULL CHECK(IN ('string','number','boolean','json'))" },
          "description": { "type": "TEXT", "constraints": "NULL" },
          "updated_at": { "type": "TEXT", "constraints": "NOT NULL DEFAULT (datetime('now'))" }
        },
        "default_settings": {
          "company_name": "شركة قطع الغيار",
          "company_name_fr": "Entreprise Pieces de Rechange",
          "company_address": "",
          "company_phone": "",
          "company_logo_path": "",
          "currency": "دج",
          "currency_fr": "DA",
          "decimal_places": "2",
          "language": "ar",
          "invoice_print_size": "A4",
          "auto_backup_enabled": "true",
          "auto_backup_time": "23:00",
          "backup_retention_days": "30",
          "sync_interval_seconds": "30",
          "tax_percent": "0",
          "low_stock_warning": "true"
        }
      }
    }
  },

  "business_calculations": {
    "description": "كل الحسابات المالية بدقة رياضية كاملة",
    "precision": "2 decimal places - استخدم Math.round(value * 100) / 100 دائماً",
    "never_use": "floating point arithmetic directly - استخدم دالة roundTo2 دائماً",

    "invoice_calculations": {
      "item_discount_amount": {
        "if_percent": "item.unit_price * item.quantity * (item.discount_value / 100)",
        "if_amount": "item.discount_value",
        "formula": "item_discount_amount = roundTo2(item_discount)"
      },
      "item_total": {
        "formula": "item.total = roundTo2((item.unit_price * item.quantity) - item.discount_amount)"
      },
      "subtotal": {
        "formula": "subtotal = roundTo2(SUM(item.total for all items))"
      },
      "global_discount_amount": {
        "if_percent": "roundTo2(subtotal * (global_discount_value / 100))",
        "if_amount": "roundTo2(global_discount_value)"
      },
      "total_before_tax": {
        "formula": "total_before_tax = roundTo2(subtotal - global_discount_amount)"
      },
      "tax_amount": {
        "formula": "tax_amount = roundTo2(total_before_tax * (tax_percent / 100))"
      },
      "total": {
        "formula": "total = roundTo2(total_before_tax + tax_amount)"
      },
      "remaining": {
        "formula": "remaining = roundTo2(total - paid)"
      }
    },

    "profit_calculations": {
      "item_gross_profit": {
        "formula": "item.gross_profit = roundTo2((item.unit_price - item.purchase_price_snapshot) * item.quantity - item.discount_amount)",
        "note": "استخدم purchase_price_snapshot وليس السعر الحالي"
      },
      "invoice_gross_profit": {
        "formula": "invoice.gross_profit = roundTo2(SUM(item.gross_profit) - global_discount_amount)"
      },
      "net_profit": {
        "formula": "net_profit = roundTo2(gross_profit - expenses_for_period)",
        "note": "المصروفات تُحسب حسب الفترة الزمنية المطلوبة"
      },
      "profit_margin_percent": {
        "formula": "profit_margin = roundTo2((net_profit / total_revenue) * 100)",
        "edge_case": "إذا كان total_revenue = 0 فالنتيجة = 0"
      },
      "branch_profit": {
        "formula": "SUM(invoice.gross_profit) لكل الفواتير المؤكدة في الفرع خلال الفترة - SUM(expenses) لنفس الفرع والفترة"
      }
    },

    "stock_calculations": {
      "stock_after_sale": "stock.quantity = roundTo2(stock.quantity - item.quantity)",
      "stock_after_purchase": "stock.quantity = roundTo2(stock.quantity + item.quantity)",
      "stock_after_supply_shipped": "hq_stock.quantity = roundTo2(hq_stock.quantity - item.quantity_shipped)",
      "stock_after_supply_confirmed": "branch_stock.quantity = roundTo2(branch_stock.quantity + item.quantity_received)",
      "discrepancy": "item.discrepancy = roundTo2(item.quantity_shipped - item.quantity_received)"
    },

    "balance_calculations": {
      "customer_balance_after_sale": "customer.balance = roundTo2(customer.balance + invoice.remaining)",
      "customer_balance_after_payment": "customer.balance = roundTo2(customer.balance - payment.amount)",
      "supplier_balance_after_purchase": "supplier.balance = roundTo2(supplier.balance + invoice.remaining)",
      "supplier_balance_after_payment": "supplier.balance = roundTo2(supplier.balance - payment.amount)"
    }
  },

  "ui_screens": {
    "screen_001_login": {
      "name": "شاشة تسجيل الدخول",
      "route": "/login",
      "access": "عامة - لا تتطلب مصادقة",
      "description": "أول شاشة تظهر عند فتح التطبيق",
      "layout": "وسط الشاشة - بطاقة زجاجية على خلفية داكنة",
      "elements": {
        "logo": { "type": "image", "src": "assets/logo.png", "size": "120px" },
        "app_title": { "type": "h1", "text": "Spare Parts ERP", "style": "text-2xl font-bold text-white" },
        "username_input": { "type": "text input", "label": "اسم المستخدم", "placeholder": "أدخل اسم المستخدم", "validation": "required min:3 max:50", "autofocus": true },
        "password_input": { "type": "password input", "label": "كلمة المرور", "placeholder": "أدخل كلمة المرور", "validation": "required min:6", "show_hide_toggle": true },
        "login_button": { "type": "button", "text": "دخول", "style": "primary full-width", "shortcut": "Enter", "loading_state": true }
      },
      "behavior": {
        "on_submit": "استدعاء authenticate_user(username, password)",
        "on_success": "حفظ token في electron-store وتوجيه لـ /dashboard",
        "on_wrong_password": "رسالة خطأ: كلمة المرور غير صحيحة",
        "on_user_not_found": "رسالة خطأ: المستخدم غير موجود",
        "on_user_inactive": "رسالة خطأ: الحساب معطل - تواصل مع المسؤول",
        "max_attempts": 5,
        "lockout_after_max_attempts": "قفل 5 دقائق مع عداد تنازلي"
      }
    },

    "screen_002_dashboard": {
      "name": "الواجهة الرئيسية",
      "route": "/dashboard",
      "access": "كل المستخدمين المسجلين",
      "description": "صفحة البداية بعد تسجيل الدخول - 3 أزرار رئيسية + معلومات مخزون",
      "layout": "Sidebar يسار + TopBar أعلى + محتوى رئيسي",

      "top_bar": {
        "left_side": {
          "element_1": { "type": "text", "content": "اسم الفرع الحالي", "icon": "Building2", "style": "pill badge" },
          "element_2": { "type": "text", "content": "اسم المستخدم + دوره", "icon": "User" },
          "element_3": { "type": "toggle", "options": ["Ar", "Fr"], "action": "toggleLanguage()", "style": "pill" }
        },
        "right_side": {
          "element_1": { "type": "indicator", "content": "حالة الاتصال السحابي", "icon": "Cloud", "colors": { "online": "green", "offline": "red", "syncing": "blue" } },
          "element_2": { "type": "badge", "content": "عدد العمليات غير المتزامنة", "show_when": "sync_queue.count > 0", "color": "amber" },
          "element_3": { "type": "text", "content": "التاريخ والوقت الحالي", "realtime": true }
        }
      },

      "main_content": {
        "three_main_buttons": {
          "button_sale": {
            "label_ar": "عملية بيع",
            "label_fr": "Opération de Vente",
            "shortcut": "F6",
            "icon": "ShoppingCart",
            "color": "blue",
            "size": "large card",
            "action": "navigate('/sales/new')",
            "visible_to": ["owner", "manager", "employee"]
          },
          "button_purchase": {
            "label_ar": "عملية شراء",
            "label_fr": "Opération d'Achat",
            "shortcut": "F7",
            "icon": "Truck",
            "color": "green",
            "size": "large card",
            "action": "navigate('/purchases/new')",
            "visible_to": ["owner", "manager", "accountant"]
          },
          "button_supply": {
            "label_ar": "تموين الفروع",
            "label_fr": "Approvisionnement",
            "shortcut": "F8",
            "icon": "Building",
            "color": "purple",
            "size": "large card",
            "action": "navigate('/branch-supply/new')",
            "visible_to": ["owner", "manager"]
          }
        },

        "info_panel": {
          "description": "معلومات المخزون فقط - لا أرباح ولا أرصدة مالية حساسة",
          "cards": {
            "low_stock_card": { "label": "منتجات ناقصة", "query": "SELECT COUNT(*) FROM stock s JOIN products p ON s.product_id=p.id WHERE s.quantity <= p.min_stock_level AND p.is_active=1", "icon": "AlertTriangle", "color": "amber", "action": "navigate('/inventory?filter=low_stock')" },
            "todays_sales_count": { "label": "فواتير اليوم", "query": "SELECT COUNT(*) FROM sales_invoices WHERE date=TODAY AND status='confirmed' AND branch_id=CURRENT_BRANCH", "icon": "Receipt", "color": "blue", "note": "عدد فقط - لا مبالغ" },
            "pending_supply_orders": { "label": "طلبات تموين معلقة", "query": "SELECT COUNT(*) FROM branch_supply_orders WHERE status IN ('pending','shipped')", "icon": "Package", "color": "purple" },
            "alerts_count": { "label": "تنبيهات النظام", "content": "منتجات منتهية + طلبات معلقة + مزامنة فاشلة", "icon": "Bell", "color": "red" }
          },
          "hidden_data": ["الأرباح", "الخسائر", "الرصيد النقدي", "إجمالي المبيعات بالمبالغ", "ديون الزبائن والموردين"]
        }
      },

      "sidebar": {
        "items": [
          { "id": "dashboard", "label_ar": "لوحة التحكم", "label_fr": "Tableau de Bord", "icon": "Home", "route": "/dashboard", "roles": ["owner", "manager", "accountant", "employee"] },
          { "id": "sales", "label_ar": "المبيعات", "label_fr": "Ventes", "icon": "ShoppingCart", "route": "/sales", "roles": ["owner", "manager", "accountant", "employee"] },
          { "id": "purchases", "label_ar": "المشتريات", "label_fr": "Achats", "icon": "Truck", "route": "/purchases", "roles": ["owner", "manager", "accountant"] },
          { "id": "inventory", "label_ar": "المخزون", "label_fr": "Stock", "icon": "Package", "route": "/inventory", "roles": ["owner", "manager", "accountant"] },
          { "id": "branches", "label_ar": "الفروع والتموين", "label_fr": "Succursales", "icon": "Building2", "route": "/branches", "roles": ["owner", "manager"] },
          { "id": "customers", "label_ar": "الزبائن", "label_fr": "Clients", "icon": "Users", "route": "/customers", "roles": ["owner", "manager", "accountant"] },
          { "id": "suppliers", "label_ar": "الموردون", "label_fr": "Fournisseurs", "icon": "UserCheck", "route": "/suppliers", "roles": ["owner", "manager", "accountant"] },
          { "id": "finance", "label_ar": "المالية والأرباح", "label_fr": "Finance", "icon": "TrendingUp", "route": "/finance", "roles": ["owner"], "protected": true, "pin_required": true, "note": "مخفية تماماً لغير المالك" },
          { "id": "reports", "label_ar": "التقارير", "label_fr": "Rapports", "icon": "BarChart3", "route": "/reports", "roles": ["owner", "manager", "accountant"] },
          { "id": "settings", "label_ar": "الإعدادات", "label_fr": "Paramètres", "icon": "Settings", "route": "/settings", "roles": ["owner"] },
          { "id": "logout", "label_ar": "تسجيل الخروج", "label_fr": "Déconnexion", "icon": "LogOut", "action": "logout()", "roles": ["owner", "manager", "accountant", "employee"], "position": "bottom" }
        ]
      }
    },

    "screen_003_new_sale": {
      "name": "فاتورة بيع جديدة",
      "route": "/sales/new",
      "shortcut_to_open": "F6 من أي مكان",
      "access": ["owner", "manager", "employee"],
      "description": "إنشاء فاتورة بيع جديدة مع بحث سريع بالباركود",

      "header_section": {
        "invoice_number": { "type": "auto-generated readonly", "position": "يسار", "note": "لا يمكن تعديله" },
        "date": { "type": "date picker", "default": "today", "editable": true },
        "branch": { "type": "readonly", "value": "الفرع الحالي للمستخدم" },
        "sale_type": { "type": "radio buttons or dropdown", "options": [{ "value": "retail", "label_ar": "تجزئة", "label_fr": "Détail" }, { "value": "wholesale", "label_ar": "جملة", "label_fr": "Gros" }, { "value": "half_wholesale", "label_ar": "نصف جملة", "label_fr": "Demi-Gros" }], "default": "retail", "effect": "تغيير sale_type يعيد حساب أسعار كل بنود الفاتورة تلقائياً" },
        "customer_search": { "type": "search input with autocomplete", "placeholder_ar": "بحث عن زبون أو اتركه فارغاً للبيع النقدي", "search_by": ["name", "code", "phone"], "min_chars_to_search": 2, "can_add_new_customer": true, "shortcut": "F3" }
      },

      "product_search_section": {
        "search_input": { "type": "text input + barcode camera icon", "placeholder_ar": "ابحث عن منتج أو امسح الباركود...", "shortcut": "F2", "search_by": ["barcode", "name", "name_fr", "internal_code"], "min_chars": 1, "search_debounce_ms": 300 },
        "search_results_dropdown": { "shows": ["اسم المنتج", "الباركود", "السعر حسب نوع البيع", "المخزون المتاح"], "max_results": 10, "select_action": "addProductToInvoice(product_id)" }
      },

      "invoice_table": {
        "columns": [
          { "key": "row_number", "label": "#", "width": "40px" },
          { "key": "product_name", "label_ar": "البيان", "label_fr": "Désignation", "width": "auto" },
          { "key": "wholesale_price", "label_ar": "سعر الجملة", "width": "100px" },
          { "key": "retail_price", "label_ar": "سعر التجزئة", "width": "100px" },
          { "key": "unit_price", "label_ar": "السعر المطبق", "width": "100px", "editable": true },
          { "key": "discount", "label_ar": "الخصم %", "width": "80px", "editable": true },
          { "key": "quantity", "label_ar": "الكمية", "width": "80px", "editable": true },
          { "key": "total", "label_ar": "المجموع", "width": "100px", "readonly": true },
          { "key": "delete", "label": "", "width": "40px", "action": "removeItem(item_id)" }
        ],
        "behavior": {
          "edit_quantity": "Double click أو Enter للتعديل المباشر",
          "quantity_validation": "لا تسمح بكمية > المخزون المتاح - تحذير",
          "recalculate_on_change": "أعد حساب total عند تغيير quantity أو discount أو unit_price",
          "barcode_scan_while_table_focused": "أضف المنتج للفاتورة مباشرة"
        }
      },

      "footer_section": {
        "left_side": {
          "notes": { "type": "textarea", "label_ar": "ملاحظات", "rows": 2 },
          "global_discount": { "type": "input with type toggle", "toggle_options": ["percent", "amount"], "label_ar": "خصم عام" }
        },
        "right_side": {
          "subtotal": { "label_ar": "المجموع الفرعي", "readonly": true },
          "global_discount_amount": { "label_ar": "قيمة الخصم", "readonly": true },
          "tax_amount": { "label_ar": "الضريبة", "readonly": true, "show_when": "tax_percent > 0" },
          "total": { "label_ar": "المجموع النهائي", "readonly": true, "style": "large bold highlighted" },
          "paid": { "label_ar": "المبلغ المدفوع", "type": "number input", "editable": true, "default": "same as total" },
          "remaining": { "label_ar": "المتبقي", "readonly": true, "color_logic": { "zero": "green", "positive": "red", "negative": "amber - استلمنا أكثر" } }
        }
      },

      "action_buttons": {
        "new": { "label_ar": "جديد", "shortcut": "F1", "action": "resetForm()", "confirm_if_unsaved": true },
        "open": { "label_ar": "فتح", "shortcut": "F2", "action": "openInvoiceSearch()" },
        "save_draft": { "label_ar": "حفظ مسودة", "shortcut": "F3", "action": "saveDraft()" },
        "confirm": { "label_ar": "تأكيد وحفظ", "shortcut": "F5", "action": "confirmInvoice()", "note": "بعد التأكيد يخصم من المخزون" },
        "print": { "label_ar": "طباعة", "shortcut": "F4", "action": "printInvoice()", "requires": "invoice must be confirmed" },
        "delete": { "label_ar": "حذف", "shortcut": "Delete", "action": "deleteInvoice()", "confirm_required": true, "roles": ["owner", "manager"] }
      }
    },

    "screen_004_sales_list": {
      "name": "قائمة فواتير البيع",
      "route": "/sales",
      "access": ["owner", "manager", "accountant", "employee"],
      "elements": {
        "filters_bar": {
          "date_from": "من تاريخ",
          "date_to": "إلى تاريخ",
          "branch": "الفرع - يظهر فقط إذا كان المستخدم owner",
          "customer": "الزبون",
          "sale_type": "نوع البيع",
          "status": "الحالة",
          "search": "بحث برقم الفاتورة"
        },
        "summary_bar": {
          "total_invoices": "عدد الفواتير",
          "total_amount": "إجمالي المبالغ - يظهر فقط لـ owner و accountant",
          "total_paid": "إجمالي المدفوع",
          "total_remaining": "إجمالي المتبقي"
        },
        "table": {
          "columns": ["رقم الفاتورة", "التاريخ", "الزبون", "نوع البيع", "المجموع", "المدفوع", "المتبقي", "الحالة", "المستخدم", "الإجراءات"],
          "pagination": "50 سجل في الصفحة",
          "row_actions": {
            "view": "عرض تفاصيل الفاتورة كاملة",
            "print": "طباعة",
            "cancel": "إلغاء - يتطلب سبب - يسترجع المخزون - roles: owner,manager"
          }
        }
      }
    },

    "screen_005_new_purchase": {
      "name": "فاتورة شراء جديدة",
      "route": "/purchases/new",
      "shortcut_to_open": "F7",
      "access": ["owner", "manager", "accountant"],
      "description": "نفس هيكل فاتورة البيع مع الاختلافات التالية",
      "differences_from_sale": {
        "supplier_instead_of_customer": true,
        "supplier_invoice_number_field": "حقل إضافي لرقم فاتورة المورد الأصلية",
        "no_sale_type": "لا يوجد نوع بيع",
        "updates_stock_up": "يزيد المخزون بدل أن ينقص",
        "updates_purchase_price": "عند التأكيد يحدّث purchase_price في جدول products للقيمة الجديدة",
        "columns_different": ["البيان", "الكمية", "سعر الوحدة", "المجموع"]
      }
    },

    "screen_006_inventory": {
      "name": "إدارة المخزون",
      "route": "/inventory",
      "access": ["owner", "manager", "accountant"],
      "tabs": {
        "tab_products": {
          "name": "المنتجات",
          "elements": {
            "search": "بحث بالاسم أو الباركود",
            "filters": { "category": "التصنيف", "stock_status": ["الكل", "ناقص", "منتهي", "كافٍ"], "branch": "الفرع" },
            "add_product_button": { "action": "navigate('/inventory/new')", "roles": ["owner", "manager"] },
            "table": {
              "columns": ["الباركود", "المنتج", "التصنيف", "سعر الشراء", "سعر التجزئة", "سعر الجملة", "المخزون", "الحد الأدنى", "الحالة", "إجراءات"],
              "note": "أسعار الشراء مخفية للموظف العادي"
            }
          }
        },
        "tab_stock_movements": {
          "name": "حركة المخزون",
          "filters": { "product": "منتج محدد", "branch": "فرع", "movement_type": ["بيع", "شراء", "تموين", "تصحيح يدوي"], "date_range": "فترة" },
          "table_columns": ["التاريخ", "النوع", "المرجع", "الكمية", "المخزون قبل", "المخزون بعد", "المستخدم"]
        },
        "tab_inventory_adjustment": {
          "name": "تصحيح المخزون",
          "access": ["owner", "manager"],
          "description": "تعديل يدوي للمخزون مع سبب إلزامي",
          "fields": { "product": "المنتج", "branch": "الفرع", "new_quantity": "الكمية الجديدة", "reason": "السبب - إلزامي" }
        }
      }
    },

    "screen_007_branch_supply": {
      "name": "تموين الفروع",
      "route": "/branch-supply",
      "access": ["owner", "manager"],
      "tabs": {
        "tab_new_order": {
          "name": "أمر تموين جديد",
          "fields": {
            "order_number": "تلقائي",
            "source_branch": "المخزن الرئيسي HQ",
            "target_branch": "الفرع المستقبل",
            "date": "التاريخ"
          },
          "table_columns": ["المنتج", "مخزون HQ الحالي", "مخزون الفرع الحالي", "الكمية المطلوبة", "ملاحظة"],
          "buttons": {
            "save_draft": "حفظ مسودة",
            "ship": { "label": "شحن وإرسال", "action": "shipOrder()", "effect": "يخصم من مخزون HQ فوراً - يغير status لـ shipped" },
            "print_delivery_note": "طباعة وصل التسليم"
          }
        },
        "tab_confirm_reception": {
          "name": "تأكيد الاستلام",
          "description": "يستخدمها مدير الفرع المستقبل لتأكيد الكميات المستلمة",
          "table_columns": ["المنتج", "الكمية المرسلة", "الكمية المستلمة فعلياً", "الفرق"],
          "discrepancy_highlight": "لوّن الصفوف التي فيها فرق باللون الأحمر",
          "confirm_button": { "action": "confirmReception()", "effect": "يضيف quantity_received لمخزون الفرع - يحفظ discrepancy" }
        },
        "tab_supply_history": {
          "name": "سجل التموين",
          "filters": ["الفرع", "التاريخ", "الحالة"],
          "table_columns": ["رقم الأمر", "من", "إلى", "التاريخ", "عدد المنتجات", "الحالة", "فروقات", "إجراءات"]
        }
      }
    },

    "screen_008_finance": {
      "name": "المالية والأرباح",
      "route": "/finance",
      "access": ["owner"],
      "pin_required": true,
      "pin_description": "يطلب PIN منفصل عن كلمة المرور عند كل دخول",
      "tabs": {
        "tab_dashboard": {
          "name": "ملخص مالي",
          "cards": {
            "revenue_today": "إيرادات اليوم",
            "revenue_month": "إيرادات الشهر",
            "gross_profit_month": "الربح الإجمالي",
            "expenses_month": "المصروفات",
            "net_profit_month": "صافي الربح",
            "profit_margin": "هامش الربح %",
            "cash_in_hand": "النقدية",
            "total_receivables": "ذمم الزبائن",
            "total_payables": "ذمم الموردين"
          },
          "chart": "رسم بياني Recharts - مبيعات وأرباح آخر 30 يوم"
        },
        "tab_profit_analysis": {
          "name": "تحليل الأرباح",
          "filters": ["من تاريخ", "إلى تاريخ", "الفرع"],
          "breakdown_by": ["المنتج", "الفئة", "الفرع", "المستخدم", "نوع البيع"]
        },
        "tab_expenses": {
          "name": "المصروفات",
          "add_expense_form": { "fields": ["التصنيف", "الوصف", "المبلغ", "التاريخ", "الفرع", "رقم الوثيقة"] }
        },
        "tab_debts": {
          "name": "إدارة الذمم",
          "sub_tabs": {
            "customer_debts": {
              "columns": ["الزبون", "الرصيد", "آخر دفعة", "تاريخ آخر فاتورة", "إجراءات"],
              "row_actions": { "add_payment": "تسجيل دفعة", "view_statement": "كشف حساب كامل", "print": "طباعة" }
            },
            "supplier_debts": { "same_structure": true }
          }
        }
      }
    },

    "screen_009_reports": {
      "name": "التقارير",
      "route": "/reports",
      "access": ["owner", "manager", "accountant"],
      "available_reports": {
        "report_daily_sales": {
          "name_ar": "تقرير المبيعات اليومي",
          "filters": ["التاريخ", "الفرع", "المستخدم"],
          "export_options": ["طباعة A4", "طباعة A5", "PDF", "Excel"]
        },
        "report_inventory": {
          "name_ar": "تقرير المخزون",
          "filters": ["الفرع", "التصنيف", "الحالة"],
          "export_options": ["طباعة A4", "Excel"]
        },
        "report_profit_loss": {
          "name_ar": "تقرير الربح والخسارة",
          "access": ["owner"],
          "filters": ["من تاريخ", "إلى تاريخ", "الفرع"],
          "export_options": ["طباعة A4", "PDF", "Excel"]
        },
        "report_customer_statement": {
          "name_ar": "كشف حساب زبون",
          "filters": ["الزبون", "من تاريخ", "إلى تاريخ"],
          "export_options": ["طباعة A4", "PDF"]
        },
        "report_supplier_statement": {
          "name_ar": "كشف حساب مورد",
          "filters": ["المورد", "من تاريخ", "إلى تاريخ"],
          "export_options": ["طباعة A4", "PDF"]
        },
        "report_branch_comparison": {
          "name_ar": "مقارنة الفروع",
          "access": ["owner"],
          "chart": "Bar Chart - Recharts",
          "export_options": ["PDF"]
        },
        "report_top_products": {
          "name_ar": "أكثر المنتجات مبيعاً",
          "filters": ["من تاريخ", "إلى تاريخ", "الفرع", "الحد الأعلى للنتائج"],
          "export_options": ["طباعة A4", "Excel"]
        },
        "report_audit_log": {
          "name_ar": "سجل العمليات",
          "access": ["owner"],
          "filters": ["المستخدم", "العملية", "التاريخ"],
          "export_options": ["Excel"]
        }
      }
    },

    "screen_010_settings": {
      "name": "الإعدادات",
      "route": "/settings",
      "access": ["owner"],
      "tabs": {
        "company": { "fields": ["اسم الشركة", "اسم الشركة بالفرنسية", "العنوان", "الهاتف", "البريد الإلكتروني", "الشعار"] },
        "branches_management": { "actions": ["إضافة فرع", "تعديل فرع", "تعطيل فرع"] },
        "users_management": { "actions": ["إضافة مستخدم", "تعديل مستخدم", "تغيير كلمة المرور", "تعطيل مستخدم"] },
        "backup": {
          "auto_backup_toggle": "تفعيل/تعطيل",
          "backup_time": "وقت النسخ الاحتياطي",
          "manual_backup_button": "نسخ احتياطي الآن",
          "restore_button": "استعادة من نسخة احتياطية",
          "backup_list": "قائمة النسخ الاحتياطية المحلية"
        },
        "printing": { "fields": ["حجم الفاتورة الافتراضي A4/A5/Thermal", "نص الرأس", "نص التذييل", "إظهار الشعار"] },
        "financial": { "fields": ["العملة", "عدد الخانات العشرية", "نسبة الضريبة الافتراضية"] },
        "sync": { "fields": ["Supabase URL", "Supabase Key", "فترة المزامنة بالثواني"], "test_connection_button": "اختبار الاتصال" }
      }
    }
  },

  "permissions_matrix": {
    "description": "جدول كامل لصلاحيات كل دور",
    "roles": {
      "owner": {
        "label": "المالك / مسؤول النظام",
        "access_all_branches": true,
        "can_see_profits": true,
        "can_see_purchase_prices": true,
        "can_delete": true,
        "can_cancel_invoice": true,
        "can_add_users": true,
        "can_see_audit_log": true,
        "can_restore_backup": true,
        "can_see_finance_module": true,
        "pin_required_for_finance": true
      },
      "manager": {
        "label": "مدير الفرع",
        "access_own_branch_only": true,
        "can_see_profits": false,
        "can_see_purchase_prices": true,
        "can_delete": false,
        "can_cancel_invoice": true,
        "can_add_users": false,
        "can_see_audit_log": false,
        "can_restore_backup": false,
        "can_see_finance_module": false
      },
      "accountant": {
        "label": "المحاسب",
        "access_own_branch_only": true,
        "can_see_profits": false,
        "can_see_purchase_prices": true,
        "can_delete": false,
        "can_cancel_invoice": false,
        "can_add_users": false,
        "can_see_audit_log": false,
        "can_restore_backup": false,
        "can_see_finance_module": false,
        "can_view_reports": true,
        "can_add_payment": true
      },
      "employee": {
        "label": "موظف",
        "access_own_branch_only": true,
        "can_see_profits": false,
        "can_see_purchase_prices": false,
        "can_delete": false,
        "can_cancel_invoice": false,
        "can_add_users": false,
        "can_see_audit_log": false,
        "can_restore_backup": false,
        "can_see_finance_module": false,
        "can_create_sale_invoice": true,
        "can_view_own_invoices": true
      }
    }
  },

  "local_storage_operations": {
    "save_flow": {
      "step_1_validation": "Zod schema validation على مستوى React",
      "step_2_ipc_call": "إرسال عبر Electron IPC: window.electronAPI.saveInvoice(data)",
      "step_3_main_process": "Main Process يستقبل الطلب",
      "step_4_begin_transaction": "db.transaction() في better-sqlite3",
      "step_5_execute_inserts": "تنفيذ كل INSERT و UPDATE داخل Transaction",
      "step_6_add_to_sync_queue": "INSERT في sync_queue",
      "step_7_add_to_audit_log": "INSERT في audit_log",
      "step_8_commit": "Commit Transaction",
      "step_9_return_result": "إرجاع { success: true, id: newId } للـ Renderer",
      "step_10_update_ui": "تحديث Zustand Store وإظهار رسالة نجاح",
      "on_any_error": "Rollback التلقائي لـ better-sqlite3 - إرجاع { success: false, error: message } - إظهار رسالة خطأ"
    },

    "read_flow": {
      "step_1": "React component يطلب البيانات عبر IPC",
      "step_2": "Main Process ينفذ SELECT على SQLite",
      "step_3": "إرجاع النتيجة للـ Renderer",
      "step_4": "تحديث الـ State المحلي للـ component",
      "caching": "استخدم Zustand لتخزين البيانات المتكررة الطلب كالمنتجات والزبائن"
    },

    "database_initialization": {
      "on_first_run": [
        "إنشاء مجلد FaroukERP في AppData",
        "إنشاء ملف farouk_erp.db",
        "تشغيل كل migration scripts بالترتيب",
        "إدراج البيانات الافتراضية في app_settings",
        "إنشاء فرع رئيسي HQ تلقائياً",
        "إنشاء مستخدم admin افتراضي username:admin password:admin123 role:owner",
        "إظهار تنبيه للمستخدم بتغيير كلمة المرور الافتراضية"
      ],
      "on_update": "تشغيل migrations الجديدة فقط التي لم تُنفّذ بعد"
    }
  },

  "cloud_sync_system": {
    "provider": "Supabase",
    "sync_strategy": "Local-First - السجلات تُحفظ محلياً أولاً ثم ترفع للسحابة",

    "sync_trigger": {
      "automatic_interval": "كل 30 ثانية إذا كان هناك اتصال",
      "on_internet_reconnect": "فور عودة الاتصال",
      "on_app_start": "عند بدء التطبيق",
      "manual": "زر مزامنة يدوية في الإعدادات"
    },

    "sync_process": {
      "step_1": "فحص navigator.onLine وتجربة اتصال فعلي لـ Supabase",
      "step_2": "جلب sync_queue WHERE status='pending' ORDER BY priority ASC, created_at ASC LIMIT 50",
      "step_3": "لكل سجل في القائمة: UPDATE status='processing'",
      "step_4": "إرسال البيانات لـ Supabase API حسب operation (INSERT/UPDATE/DELETE)",
      "step_5": "انتظار تأكيد Supabase (acknowledgment)",
      "step_6_on_success": "UPDATE sync_queue SET status='done' - UPDATE record SET synced=1 - UPDATE cloud_id",
      "step_7_on_failure": "UPDATE sync_queue SET attempts=attempts+1, last_error=error_message, status='pending'",
      "step_8_max_attempts": "إذا attempts >= max_attempts: UPDATE status='failed' - إشعار للمستخدم"
    },

    "conflict_resolution": {
      "strategy": "Last-Write-Wins مع timestamp",
      "rule": "السجل الأحدث updated_at يفوز دائماً",
      "local_vs_cloud": "إذا كان السجل المحلي أحدث من السجل في السحابة: رفع المحلي - إذا السحابة أحدث: تحديث المحلي",
      "note": "في حالة التعارض الحقيقي: احتفظ بالمحلي وسجّل التعارض في audit_log"
    },

    "offline_behavior": {
      "all_operations_work_locally": true,
      "sync_indicator_in_topbar": "يظهر عدد العمليات المنتظرة",
      "user_notification": "إشعار بسيط عند عودة الاتصال والمزامنة",
      "no_blocking": "لا تمنع أي عملية بسبب عدم الاتصال"
    },

    "data_not_synced": {
      "audit_log": "لا يُزامَن - محلي فقط",
      "sync_queue": "لا يُزامَن - داخلي فقط",
      "app_settings": "مزامنة انتقائية لبعض الإعدادات فقط"
    }
  },

  "backup_system": {
    "auto_backup": {
      "enabled_by_default": true,
      "default_time": "23:00",
      "process": [
        "نسخ ملف farouk_erp.db",
        "حفظه في مجلد backups/ باسم farouk_erp_backup_{datetime}.db",
        "حذف النسخ الأقدم من 30 يوماً",
        "إذا كان Supabase متصل: رفع النسخة لـ Supabase Storage أيضاً"
      ]
    },
    "manual_backup": {
      "user_chooses_location": true,
      "default_location": "Desktop",
      "format": "farouk_erp_backup_{datetime}.db"
    },
    "restore_process": {
      "step_1": "تحذير المستخدم أن الاستعادة ستحذف البيانات الحالية",
      "step_2": "طلب تأكيد مزدوج",
      "step_3": "إغلاق اتصال قاعدة البيانات",
      "step_4": "نسخ ملف الاستعادة بديلاً عن الملف الحالي",
      "step_5": "إعادة فتح الاتصال",
      "step_6": "التحقق من سلامة قاعدة البيانات",
      "step_7": "إعادة تشغيل التطبيق"
    }
  },

  "printing_system": {
    "supported_sizes": ["A4", "A5", "Thermal 80mm"],
    "templates": {
      "sale_invoice_a4": {
        "header": "شعار الشركة + اسمها + عنوانها + هاتفها",
        "invoice_info": "رقم الفاتورة + التاريخ + اسم الزبون",
        "table": "كل بنود الفاتورة",
        "footer": "المجموع + الخصم + الإجمالي + المدفوع + المتبقي + ملاحظات + توقيع"
      },
      "sale_invoice_a5": "نفس A4 لكن بحجم أصغر",
      "delivery_note": "وصل التسليم لتموين الفروع",
      "customer_statement": "كشف حساب الزبون",
      "inventory_report": "تقرير المخزون"
    },
    "process": {
      "step_1": "React يولّد HTML template للفاتورة",
      "step_2": "استدعاء react-to-print",
      "step_3": "Electron يفتح نافذة الطباعة",
      "step_4": "UPDATE printed_count += 1 في قاعدة البيانات",
      "step_5": "INSERT في audit_log بعملية PRINT"
    },
    "rtl_support": "كل القوالب تدعم RTL للعربية"
  },

  "i18n_system": {
    "default_language": "ar",
    "supported_languages": ["ar", "fr"],
    "rtl_languages": ["ar"],
    "ltr_languages": ["fr"],
    "library": "react-i18next",
    "library_version": "14.0.0",
    "toggle_location": "TopBar - يحفظ الاختيار في app_settings",
    "what_changes_with_language": ["نصوص الواجهة", "اتجاه النص RTL/LTR", "اتجاه Layout", "تنسيق التاريخ", "تنسيق الأرقام"],
    "what_stays_same": "البيانات المدخلة - تبقى بلغتها الأصلية"
  },

  "error_handling": {
    "categories": {
      "validation_errors": {
        "display": "تحت الحقل المعني مباشرة باللون الأحمر",
        "examples": { "required_field": "هذا الحقل إلزامي", "price_invalid": "السعر يجب أن يكون أكبر من 0", "quantity_exceeds_stock": "الكمية المطلوبة تتجاوز المخزون المتاح ({available})" }
      },
      "business_logic_errors": {
        "display": "Toast notification باللون الأحمر في أعلى الشاشة",
        "examples": {
          "invoice_already_confirmed": "لا يمكن تعديل فاتورة مؤكدة",
          "insufficient_stock": "المخزون غير كافٍ للمنتج: {product_name}",
          "customer_credit_limit_exceeded": "تجاوز حد الائتمان المسموح للزبون",
          "cannot_delete_used_product": "لا يمكن حذف منتج مستخدم في فواتير - يمكن تعطيله فقط"
        }
      },
      "database_errors": {
        "display": "Modal نافذة خطأ مع تفاصيل ورسالة للدعم التقني",
        "action": "تسجيل الخطأ كاملاً في audit_log"
      },
      "sync_errors": {
        "display": "Badge في TopBar مع عدد الأخطاء",
        "detail_in": "صفحة الإعدادات > المزامنة"
      },
      "permission_errors": {
        "display": "صفحة 403 Forbidden داخل التطبيق",
        "message": "ليس لديك صلاحية الوصول لهذه الصفحة"
      }
    }
  },

  "security": {
    "authentication": {
      "token_storage": "electron-store (مشفر على disk)",
      "token_type": "JWT",
      "token_expiry": "8 ساعات",
      "session_check": "عند كل تنقل بين الصفحات",
      "auto_logout_on_expiry": true,
      "auto_logout_on_inactivity": "30 دقيقة من عدم النشاط"
    },
    "data_encryption": {
      "sensitive_fields_in_memory": "purchase_price, profit values تُشفر في Zustand Store",
      "password_storage": "bcrypt hash فقط - لا نخزن كلمة المرور أبداً",
      "cloud_transmission": "HTTPS فقط - TLS 1.3"
    },
    "input_sanitization": {
      "all_text_inputs": "sanitize قبل حفظ في قاعدة البيانات",
      "sql_injection_prevention": "استخدام Prepared Statements دائماً - never string concatenation"
    },
    "file_system": {
      "database_file_access": "Main Process فقط - لا يمكن للـ Renderer الوصول المباشر",
      "ipc_validation": "كل IPC call يتحقق من صلاحية المستخدم قبل التنفيذ"
    }
  },

  "performance_requirements": {
    "search_response_time": "< 150ms للبحث في 100,000 منتج",
    "invoice_save_time": "< 300ms للحفظ الكامل مع Transaction",
    "page_load_time": "< 500ms لكل صفحة",
    "app_startup_time": "< 4 ثوان",
    "database_optimization": {
      "indexes": "كل الفهارس المذكورة في database_schema",
      "wal_mode": "تفعيل WAL mode في SQLite",
      "pagination": "50 سجل في الصفحة - لا تجلب كل البيانات مرة واحدة",
      "virtual_scrolling": "للجداول الكبيرة > 1000 صف"
    }
  },

  "invoice_numbering": {
    "format": "{TYPE}-{BRANCH_CODE}-{YYYY}-{NNNN}",
    "examples": {
      "sale": "VNT-HQ-2024-0001",
      "purchase": "ACH-HQ-2024-0001",
      "supply": "TMN-2024-0001",
      "payment": "PAY-2024-0001",
      "expense": "EXP-2024-0001"
    },
    "sequence_rule": "التسلسل يبدأ من 0001 كل سنة لكل فرع بشكل منفصل",
    "get_next_number_function": "SELECT MAX(CAST(SUBSTR(invoice_number, -4) AS INTEGER)) + 1 FROM sales_invoices WHERE invoice_number LIKE 'VNT-{branch}-{year}-%'"
  },

  "electron_ipc_channels": {
    "description": "قنوات IPC بين Main Process و Renderer Process",
    "channels": {
      "db:users:authenticate": "تسجيل الدخول",
      "db:users:getAll": "جلب كل المستخدمين",
      "db:users:create": "إنشاء مستخدم",
      "db:users:update": "تحديث مستخدم",
      "db:products:search": "بحث في المنتجات",
      "db:products:getAll": "جلب كل المنتجات",
      "db:products:create": "إنشاء منتج",
      "db:products:update": "تحديث منتج",
      "db:sales:create": "إنشاء فاتورة بيع",
      "db:sales:confirm": "تأكيد فاتورة بيع",
      "db:sales:cancel": "إلغاء فاتورة بيع",
      "db:sales:getList": "جلب قائمة الفواتير",
      "db:sales:getById": "جلب فاتورة واحدة",
      "db:purchases:create": "إنشاء فاتورة شراء",
      "db:purchases:confirm": "تأكيد فاتورة شراء",
      "db:stock:getByBranch": "جلب مخزون فرع",
      "db:stock:getMovements": "جلب حركة المخزون",
      "db:supply:create": "إنشاء أمر تموين",
      "db:supply:ship": "شحن أمر تموين",
      "db:supply:confirmReception": "تأكيد استلام تموين",
      "db:finance:getSummary": "جلب الملخص المالي",
      "db:reports:generate": "توليد تقرير",
      "db:backup:create": "إنشاء نسخة احتياطية",
      "db:backup:restore": "استعادة نسخة احتياطية",
      "db:backup:list": "قائمة النسخ الاحتياطية",
      "print:invoice": "طباعة فاتورة",
      "print:report": "طباعة تقرير",
      "sync:triggerManual": "مزامنة يدوية",
      "sync:getStatus": "حالة المزامنة",
      "app:getVersion": "إصدار التطبيق",
      "app:openLogFolder": "فتح مجلد السجلات"
    }
  },

  "keyboard_shortcuts": {
    "F1": "فاتورة جديدة",
    "F2": "التركيز على حقل البحث عن منتج",
    "F3": "البحث عن زبون",
    "F4": "طباعة الفاتورة الحالية",
    "F5": "حفظ وتأكيد",
    "F6": "فتح فاتورة بيع جديدة من أي مكان",
    "F7": "فتح فاتورة شراء جديدة من أي مكان",
    "F8": "فتح أمر تموين جديد من أي مكان",
    "F9": "فتح البحث العالمي",
    "Escape": "إغلاق النافذة المنبثقة أو إلغاء العملية الحالية",
    "Delete": "حذف الصف المحدد في الجداول",
    "Ctrl+S": "حفظ النموذج الحالي",
    "Ctrl+P": "طباعة",
    "Ctrl+F": "بحث",
    "Ctrl+Z": "تراجع"
  },

  "build_and_deployment": {
    "development": {
      "command_start_react": "npm run dev",
      "command_start_electron": "npm run electron:dev",
      "hot_reload": "Vite HMR + Electron restart on main changes"
    },
    "production_build": {
      "command": "npm run build",
      "process": [
        "1. Vite يبني React في مجلد dist/renderer",
        "2. TypeScript يتحقق من الأنواع",
        "3. Electron Builder يلف كل شيء",
        "4. يُنتج installer في مجلد release/"
      ],
      "output_files": {
        "windows": "FaroukERP-Setup-{version}.exe",
        "installer_type": "NSIS"
      }
    },
    "auto_update": {
      "provider": "GitHub Releases أو Supabase Storage",
      "check_on_startup": true,
      "silent_download": true,
      "prompt_before_install": true
    },
    "signing": {
      "windows": "Code Signing Certificate مطلوب للإنتاج",
      "note": "بدون توقيع سيظهر تحذير Windows SmartScreen"
    }
  },

  "testing_requirements": {
    "unit_tests": {
      "framework": "Vitest",
      "must_test": [
        "كل دوال calculations.ts",
        "كل دوال formatters.ts",
        "منطق permissions.ts",
        "توليد أرقام الفواتير",
        "منطق المزامنة"
      ]
    },
    "integration_tests": {
      "framework": "Vitest + better-sqlite3 in-memory",
      "must_test": [
        "حفظ فاتورة كاملة وتأثيرها على المخزون",
        "إلغاء فاتورة وإرجاع المخزون",
        "عملية تموين الفروع كاملة",
        "حساب ذمم الزبائن"
      ]
    },
    "e2e_tests": {
      "framework": "Playwright",
      "must_test": [
        "تسجيل الدخول بنجاح وبفشل",
        "إنشاء فاتورة بيع كاملة",
        "البحث بالباركود",
        "تصفية قائمة الفواتير"
      ]
    }
  },

  "implementation_instructions": {
    "critical_rules": [
      "RULE_1: لا تستخدم أبداً string concatenation في SQL - استخدم Prepared Statements فقط",
      "RULE_2: كل عملية تمس أكثر من جدول واحد يجب أن تكون داخل Transaction",
      "RULE_3: دائماً سجّل في audit_log بعد كل عملية مهمة",
      "RULE_4: دائماً أضف لـ sync_queue بعد كل INSERT/UPDATE/DELETE",
      "RULE_5: لا تخزن كلمات المرور أبداً - bcrypt hash فقط",
      "RULE_6: تحقق من الصلاحيات في IPC handler وليس فقط في الـ UI",
      "RULE_7: استخدم roundTo2() لكل العمليات الحسابية المالية",
      "RULE_8: لا تجلب كل البيانات مرة واحدة - استخدم pagination دائماً",
      "RULE_9: purchase_price_snapshot يجب حفظه في كل بند فاتورة وقت البيع",
      "RULE_10: لا تسمح بـ quantity سالبة في المخزون - أظهر تحذيراً للمستخدم ولا تمنع البيع تلقائياً"
    ],
    "start_with": [
      "1. إعداد بنية المشروع Electron + React + TypeScript + Vite",
      "2. إنشاء قاعدة البيانات بكل الجداول والفهارس",
      "3. نظام تسجيل الدخول والصلاحيات",
      "4. الواجهة الرئيسية مع الـ Layout",
      "5. وحدة المنتجات والمخزون",
      "6. فاتورة البيع",
      "7. فاتورة الشراء",
      "8. تموين الفروع",
      "9. المالية والأرباح",
      "10. التقارير",
      "11. المزامنة السحابية",
      "12. النسخ الاحتياطي",
      "13. الطباعة",
      "14. الإعدادات",
      "15. الاختبارات والبناء النهائي"
    ]
  }
}